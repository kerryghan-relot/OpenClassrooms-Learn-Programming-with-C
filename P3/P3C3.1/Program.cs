MakeAddition(1, 2);

MakeAddition(3, 4);

MakeAddition(5, 6);

static void MakeAddition(int x, int y)
{
    Console.WriteLine("Let's try addition!");
    int z = x + y;
    Console.WriteLine("The sum is " + z);
}